function solve(num) {
    for (let i = 0; i < num; i++) {
        let line = ``;
        for (let j = 0; j < num; j++) {
            line += num + ` `;
        }
        console.log(line);
    }
}